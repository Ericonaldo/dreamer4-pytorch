# Policy eval defaults (merge with configs/walker_walk/bc.yaml)
eval:
  task: walker_walk
  image_size: 64
  action_repeat: 1
  max_episode_steps: 1000
  max_history: 16
  camera_id: -1
  proprio: false
  image: true
  episodes: 50
  num_envs: 8
